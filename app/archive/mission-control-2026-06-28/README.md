# Mission-Control UI Archive

This directory preserves the Flutter web UI state before the IDE workbench redesign.

Archived files:
- `dashboard_tab.dart`
- `home_screen.dart`
- `theme.dart`

The archived version used a dark mission-control treatment with Aladin in the dashboard hero.
